#!/usr/bin/env bash

CHECK="curl -fail --silent -XGET http://<%= @my_private_ip %>:<%= node['logstash']['http']['port']%> 2>&1 | grep -q green"

source <%= node['consul']['health-check']['retryable-check-file'] %>

_run_health_check "$CHECK"
